# 前端「唯一需要按本机修改」的配置。复制为 .env：
#   copy .env.example .env

# Mapbox 公开访问令牌（必填才能显示地图）
# 在 https://account.mapbox.com/access-tokens/ 创建
VITE_MAPBOX_TOKEN=

# 后端 API 地址（可选，默认 http://127.0.0.1:8000）
VITE_API_BASE=http://127.0.0.1:8000
